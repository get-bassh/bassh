# bassh deployment workflow
# Copy this file to your repo: .github/workflows/deploy.yml
#
# Required GitHub Secrets:
#   BASSH_API - Your bassh worker URL
#   BASSH_KEY - Your API key (get with: bassh key)
#
# Optional GitHub Secrets:
#   SITE_PASSWORD  - Password protection for the site

name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:  # Allow manual trigger

# ============================================================
# CONFIGURATION - Edit these values
# ============================================================
env:
  DEPLOY_DIR: "./public"      # Directory to deploy (e.g., ./dist, ./build, ./public)
  PROJECT_NAME: "my-site"     # Your project name (lowercase, dashes allowed)
  # CUSTOM_DOMAIN: ""         # Optional: your custom domain (e.g., docs.example.com)
# ============================================================

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Uncomment if you need to build first:
      # - name: Setup Node.js
      #   uses: actions/setup-node@v4
      #   with:
      #     node-version: '20'
      #     cache: 'npm'
      #
      # - name: Install dependencies
      #   run: npm ci
      #
      # - name: Build
      #   run: npm run build

      - name: Install bassh CLI
        run: |
          curl -fsSL https://raw.githubusercontent.com/get-bassh/bassh/main/install.sh | bash
          echo "$HOME/.local/bin" >> $GITHUB_PATH

      - name: Deploy to bassh
        env:
          BASSH_API: ${{ secrets.BASSH_API }}
          BASSH_KEY: ${{ secrets.BASSH_KEY }}
        run: |
          bassh ${{ env.DEPLOY_DIR }} -n ${{ env.PROJECT_NAME }}

      - name: Show deployment URL
        run: |
          echo "Deployed to: https://${{ secrets.BASSH_API && 'your-username' }}-${{ env.PROJECT_NAME }}.pages.dev"
